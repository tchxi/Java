// *9.4 (Use the Random class)
import java.util.*;

public class Ex9_4 {
	public static void main(String[] args) {
		int seed = 1000;
		Random r = new Random(seed);
		
		System.out.print("For seed " + seed + ": ");
		for (int i = 0; i < 50; i++)
			System.out.print(r.nextInt(100) + " ");
	}
}
